import 'package:flutter/material.dart';

class Reposts extends StatelessWidget {
  const Reposts({super.key});

  @override
  Widget build(BuildContext context) {
    return ListView(
      children: const [Center(child: Text("Reposts Page "))],
    );
  }
}
